Directories are organized by skill primarily showcased.
	There is overlap. For example, HTML is needed for CSS
	More complicated sections are likely to pull from earlier sections

There is also a projects directory. 
	Each directory inside is prefixed by section.


Client-side Javascript
	Basic Javascript
	HTML

Styling (Complex to Simple):
	Bootstrap (CSS Library)
	CSS
	HTML

